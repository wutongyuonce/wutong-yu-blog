<div
  class="tiny-desktop"
  role="img"
  aria-label="一个带有 Ideas、Notes 文件夹和动态像素小伙伴的复古桌面"
>
  <div class="tiny-desktop__menu" aria-hidden="true">
    <strong>◆</strong>
    <span>File</span>
    <span>Edit</span>
    <span>View</span>
    <time>09:41</time>
  </div>

  <div
    class="tiny-desktop__folder tiny-desktop__folder--ideas"
    aria-hidden="true"
  >
    <i></i>
    <span>Ideas</span>
  </div>
  <div
    class="tiny-desktop__folder tiny-desktop__folder--notes"
    aria-hidden="true"
  >
    <i></i>
    <span>Notes</span>
  </div>

  <div class="tiny-desktop__pet" aria-hidden="true">
    <i></i>
    <span>•ᴗ•</span>
  </div>

  <div class="tiny-desktop__dock" aria-hidden="true">
    <i></i><i></i><i></i><i></i>
  </div>
</div>

<style>
  .tiny-desktop {
    position: relative;
    width: 16.125rem;
    height: 9.875rem;
    flex: none;
    border: 2px solid color-mix(in srgb, var(--c-text) 78%, transparent);
    border-radius: 0.5rem;
    background-color: color-mix(in srgb, var(--c-bg) 82%, var(--c-text) 18%);
    background-image: linear-gradient(
      45deg,
      color-mix(in srgb, var(--c-bg) 72%, var(--c-text) 28%) 25%,
      transparent 25%,
      transparent 75%,
      color-mix(in srgb, var(--c-bg) 72%, var(--c-text) 28%) 75%
    );
    background-size: 0.5rem 0.5rem;
    box-shadow: 0.45rem 0.45rem 0
      color-mix(in srgb, var(--c-text) 15%, transparent);
    color: color-mix(in srgb, var(--c-text) 84%, transparent);
    overflow: hidden;
    transform: rotate(0.8deg);
  }

  .tiny-desktop__menu {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    height: 1.35rem;
    padding: 0 0.5rem;
    border-bottom: 2px solid currentColor;
    background: color-mix(in srgb, var(--c-bg) 94%, var(--c-text) 6%);
    font-family:
      ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.5rem;
    line-height: 1;
  }

  .tiny-desktop__menu strong {
    font-size: 0.55rem;
  }

  .tiny-desktop__menu time {
    margin-left: auto;
  }

  .tiny-desktop__folder {
    position: absolute;
    top: 2.35rem;
    display: grid;
    place-items: center;
    gap: 0.22rem;
    font-family:
      ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.5rem;
    line-height: 1;
  }

  .tiny-desktop__folder--ideas {
    left: 1.4rem;
  }

  .tiny-desktop__folder--notes {
    right: 1.4rem;
  }

  .tiny-desktop__folder i {
    position: relative;
    display: block;
    width: 2rem;
    height: 1.45rem;
    border: 2px solid currentColor;
    background: color-mix(in srgb, var(--c-bg) 86%, var(--c-text) 14%);
    box-shadow: 0.14rem 0.14rem 0
      color-mix(in srgb, var(--c-text) 35%, transparent);
  }

  .tiny-desktop__folder i::before {
    content: '';
    position: absolute;
    top: -0.42rem;
    left: -2px;
    width: 0.8rem;
    height: 0.3rem;
    border: 2px solid currentColor;
    border-bottom: 0;
    background: inherit;
  }

  .tiny-desktop__pet {
    position: absolute;
    top: 3.65rem;
    left: calc(50% - 1.95rem);
    width: 3.9rem;
    height: 2.7rem;
    border: 2px solid currentColor;
    background: color-mix(in srgb, var(--c-bg) 94%, var(--c-text) 6%);
    box-shadow: 0.16rem 0.16rem 0
      color-mix(in srgb, var(--c-text) 32%, transparent);
    font-family:
      ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    text-align: center;
    animation: tiny-desktop-pet 4s ease-in-out infinite;
  }

  .tiny-desktop__pet i {
    position: absolute;
    top: -0.42rem;
    left: 0.35rem;
    width: 0.45rem;
    height: 0.45rem;
    background: currentColor;
  }

  .tiny-desktop__pet span {
    display: block;
    margin-top: 0.7rem;
    font-size: 0.8rem;
  }

  .tiny-desktop__dock {
    position: absolute;
    bottom: 0.5rem;
    left: 50%;
    display: flex;
    gap: 0.32rem;
    padding: 0.28rem 0.4rem;
    border: 1px solid currentColor;
    background: color-mix(in srgb, var(--c-bg) 90%, var(--c-text) 10%);
    transform: translateX(-50%);
  }

  .tiny-desktop__dock i {
    width: 0.58rem;
    height: 0.58rem;
    border: 1px solid currentColor;
    background: color-mix(in srgb, var(--c-bg) 72%, var(--c-text) 28%);
  }

  .tiny-desktop__dock i:nth-child(2) {
    background: color-mix(in srgb, var(--c-bg) 52%, var(--c-text) 48%);
  }

  @keyframes tiny-desktop-pet {
    0%,
    100% {
      transform: translate(-0.45rem, 0);
    }
    45% {
      transform: translate(0.8rem, -0.3rem);
    }
    50% {
      transform: translate(0.8rem, -0.58rem);
    }
    55% {
      transform: translate(0.8rem, -0.3rem);
    }
  }

  @media (max-width: 720px) {
    .tiny-desktop {
      width: min(16.125rem, calc(100vw - 4rem));
      transform: rotate(0.5deg) scale(0.92);
      transform-origin: left top;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .tiny-desktop__pet {
      animation: none;
    }
  }
</style>
